# Auditoría de Seguimiento — Proyecto Peano

**Fecha**: 2026-07-13
**Auditor**: Claude Sonnet 5 (Claude Code)
**Alcance**: verificación de cierre de `AUDIT-2026-07-12.md` (46 incongruencias) tras los
commits `d67ce75` (export blocks/barrels/frozen_files) y `abd9ba7` (namespace por
fichero) + build real (`lake build`, `check-sorry.bash`) + barrido de conformidad
adicional (cabeceras de copyright §23).

---

## Resumen ejecutivo

De las 46 incongruencias de `AUDIT-2026-07-12.md`, la gran mayoría están **cerradas**:
export blocks, barrels temáticos, `Peano.lean` completo, `frozen_files.txt`
reescrito, namespace-por-fichero en `GroupTheory/`/`Foundation/`, ficheros
scratch/temp eliminados del historial, `NAMING-CONVENTIONS.md` ampliado (instancias +
prefijo booleano). El build real compila limpio: **73 jobs, 0 errores, 0 `sorry`**
(verificado con `lake build` y `check-sorry.bash` en esta sesión — más jobs que
cualquier cifra documentada, ver §A abajo).

Quedan **8 incongruencias abiertas** (una nueva, no detectada en la auditoría previa)
y ningún hallazgo `sorry`/error real de compilación.

---

## A — Sigue sin resolver: versión de Lean desincronizada (AUDIT-2026-07-12 §A-1)

`lean-toolchain` → `v4.30.0`. Pero:
- `README.md:8` y `README.md:220` → `v4.29.0`
- `REFERENCE.md:16` → `Lean 4 v4.29.0`

**Corrección**: sustituir `v4.29.0` → `v4.30.0` en los 3 sitios.

---

## B — Conteo de jobs desactualizado en todos los documentos

`lake build` real de esta sesión: **73 jobs · 0 errores · 0 sorry**. Ningún documento
refleja esta cifra:

| Documento | Cifra declarada |
|---|---|
| `README.md` | 66 jobs |
| `REFERENCE.md` §0.1 | 66 build jobs (fecha interna 2026-07-12, ver §D) |
| `NEXT-STEPS.md` | 66 jobs (full build) |
| `CURRENT-STATUS-PROJECT.md` | 19 jobs (marcado explícitamente como incremental, no total) |

**Corrección**: actualizar los 4 documentos a "73 jobs" con fecha 2026-07-13.

---

## C — `Perm.lean`: TODOs y placeholder no probado siguen presentes (AUDIT-2026-07-12 §I-1, sin resolver)

`Peano/PeanoNat/Combinatorics/Perm.lean`:
- Línea 66: TODO sobre descomposición en ciclos.
- Línea 74, 79: dos `-- TODO:` más (ciclos, signo de permutación).
- Líneas 67-69: `card_Sym` demuestra `factorial A.card = factorial A.card` por `rfl`
  — no demuestra `|Sym A| = |A|!` (el enunciado real). Se **exporta públicamente**
  (línea 93 del bloque `export`) como si fuera el teorema real.

Esto sigue violando AI-GUIDE.md §8 ("cero contenido no probado... cero teoremas
pendientes documentados como si estuvieran cerrados") porque `card_Sym` aparece en el
`export` público sin advertencia visible desde fuera del fichero de que es un
placeholder trivial.

**Corrección**: o se retira `card_Sym` del `export` (dejarlo `private` o eliminarlo)
hasta tener el enunciado real, o se renombra a algo que no sugiera que cuenta
permutaciones (p. ej. eliminarlo directamente — no aporta valor matemático).

---

## D — `REFERENCE.md`: fecha de cabecera inconsistente con el cuerpo del documento

- Línea 3 (cabecera del documento): `**Última actualización:** 2026-05-07`
- Línea 16 (dentro de §0.1): `*Actualizado: 2026-07-12*`

El propio fichero se contradice sobre cuándo se actualizó por última vez.

**Corrección**: unificar ambas fechas (usar la más reciente real, y actualizarla de
nuevo tras aplicar las correcciones de esta auditoría).

---

## E — `DEPENDENCIES.md`: nodo raíz ficticio "Peano" en el grafo Mermaid (AUDIT-2026-07-12 §J-3, sin resolver)

El grafo sigue teniendo `Peano --> Fermat`, `Peano --> ChineseRemainder`, etc. como si
`Peano` (el barrel raíz) dependiera de esos módulos, cuando la relación real es la
inversa (`Fermat` importa cosas, no al revés — `Peano.lean` es quien importa a
`Fermat`). La cabecera del propio fichero admite que el grafo no se ha regenerado
desde 2026-05-10.

**Corrección**: no urgente (documento ya marcado como orientativo), pero pendiente de
regeneración real si se quiere que dependa de la estructura verificada por
`gen-root.bash`.

---

## F — `CURRENT-STATUS-PROJECT.md` y `PLANNING.md` desactualizados

- `CURRENT-STATUS-PROJECT.md`: última actualización declarada **2026-05-21** — no
  refleja ni el conteo de jobs real (73), ni los fixes de namespace-por-fichero
  (`abd9ba7`), ni el cierre de export blocks/barrels/frozen_files (`d67ce75`).
- `PLANNING.md`: última actualización declarada **2026-05-09** — dos meses de
  desfase respecto a la sesión de auditoría 2026-07-12/13.

Ninguno de los dos es incorrecto en lo que dice (no hay afirmaciones falsas), pero
ambos son "snapshots en vivo" per AI-GUIDE.md §26 y llevan mucho tiempo sin tocarse
mientras el proyecto sí ha cambiado.

**Corrección**: pasada de `actualiza_documentacion` (AI-GUIDE.md) para poner ambos al
día con el estado 2026-07-13.

---

## G — `fix_sylow.py` sigue rastreado por git pese al patrón `.gitignore`

`.gitignore` incluye `fix_*.py` (añadido en el commit de auditoría 2026-07-12), pero
`git ls-files` sigue listando `fix_sylow.py` — el patrón `.gitignore` no afecta a
ficheros ya rastreados, solo previene que se añadan nuevos. Es un script de sesión en
el directorio raíz, violación residual de §16b.

**Corrección**: `git rm --cached fix_sylow.py` (y decidir si se conserva localmente
ignorado o se borra).

---

## H — NUEVO hallazgo: cabecera de copyright después del `import` en 11 ficheros (viola §23)

AI-GUIDE.md §23 exige: *"Todo fichero `.lean`, sin excepción, empieza con cabecera de
copyright antes de cualquier `import`."* Los siguientes ficheros tienen el bloque
`import` **antes** del comentario de copyright:

```
Peano/PeanoNat/Combinatorics/Group.lean
Peano/PeanoNat/Combinatorics/GroupTheory/FirstIsomorphism.lean
Peano/PeanoNat/Combinatorics/GroupTheory/QuotientGroup.lean
Peano/PeanoNat/Combinatorics/GroupTheory/SecondIsomorphism.lean
Peano/PeanoNat/Combinatorics/GroupTheory/ThirdIsomorphism.lean
Peano/PeanoNat/Combinatorics/GroupTheory/Zassenhaus.lean
Peano/PeanoNat/Combinatorics/Orbit.lean
Peano/PeanoNat/ListsAndSets/FSetFunction.lean
Peano/PeanoNat/ListsAndSets/List.lean
Peano/PeanoNat/NumberSets.lean
Peano/PeanoNat/Primes.lean
```

Todos compilan igual (Lean no exige el orden), es una cuestión puramente normativa,
pero es una violación real y no detectada en `AUDIT-2026-07-12.md`.

**Corrección**: mover el bloque `/- Copyright ... -/` antes de los `import` en los 11
ficheros — cambio mecánico, no afecta pruebas.

---

## I — Warnings del build no coinciden con la tabla de `NEXT-STEPS.md`

`NEXT-STEPS.md` (§"Warnings pendientes menores") lista: `PeanoNat.lean:28` (`n`, `m`),
`CosetAction.lean:292-294` (`h`,`r`,`h₁`,`h₂`), `Sylow.lean:2027` (`hΩ_nd`).

El `lake build` real de esta sesión solo emite 3 warnings, todos en
`SecondIsomorphism.lean:276,302,324` (`simp` argument `HNg` sin uso) — ninguno
coincide con la tabla documentada. O bien esos warnings antiguos ya no existen (se
corrigieron sin actualizar la tabla) o bien hay una discrepancia de toolchain/flags.

**Corrección**: refrescar la tabla de warnings con la salida real de `lake build`.

---

## Cosas verificadas como YA resueltas (no repetir trabajo)

- ✅ `Peano.lean` importa barrels temáticos (`ListsAndSets`, `Combinatorics`,
  `NumberTheory`, `Foundation.Foundation`) — C-1 cerrado.
- ✅ Barrels temáticos creados: `Combinatorics.lean`, `ListsAndSets.lean`,
  `NumberTheory.lean`, `Combinatorics/GroupTheory.lean`,
  `Combinatorics/GroupTheory/Sylow.lean` — G-1 cerrado.
- ✅ `Fractions.lean` tiene bloque `export Peano.Arith (...)` — B-1 cerrado.
- ✅ `frozen_files.txt` reescrito con rutas reales post-reorganización — E-1 cerrado.
- ✅ Namespace por fichero real en `GroupTheory/*.lean` y `Foundation/*.lean` (namespace
  anidado `namespace Peano / namespace <Concepto>`, verificado en disco, no solo en
  `DECISIONS.md`) — ADR-005 cerrado de verdad.
- ✅ `temp_*.lean`, `scratch_*.lean`, directorio raíz `PeanoNat/` — eliminados del
  árbol de trabajo y del índice de git; patrones añadidos a `.gitignore` — H-1/H-2/H-3
  cerrados (salvo G arriba, `fix_sylow.py`).
- ✅ `ThirdIsomorphism.lean` documentado en `README.md` y `CURRENT-STATUS-PROJECT.md`
  — D-1 cerrado.
- ✅ `NAMING-CONVENTIONS.md` §1/§8.1 documentan convención `inst` y prefijo booleano
  `b` — K-2/K-3 cerrados.
- ✅ `Counting.lean`/`Orbit.lean`/`Sign.lean` son stubs legítimos sin contenido no
  probado (namespace reservado + comentario explícito "pendiente de implementación",
  sin teoremas ni `sorry`) — no violan §8, pese a carecer de `export`.
- ✅ `check-sorry.bash` → 0 `sorry` en todo el proyecto.

---

## Resumen de acciones pendientes

| # | Severidad | Fichero(s) | Acción |
|---|---|---|---|
| A | Importante | `README.md`, `REFERENCE.md` | v4.29.0 → v4.30.0 |
| B | Importante | `README.md`, `REFERENCE.md`, `NEXT-STEPS.md`, `CURRENT-STATUS-PROJECT.md` | Actualizar a 73 jobs |
| C | Importante | `Perm.lean` | Retirar `card_Sym` del export o resolver el TODO real |
| D | Menor | `REFERENCE.md` | Unificar fecha de cabecera vs. cuerpo |
| E | Menor | `DEPENDENCIES.md` | Grafo: nodo raíz ficticio (ya marcado orientativo) |
| F | Importante | `CURRENT-STATUS-PROJECT.md`, `PLANNING.md` | Pasada `actualiza_documentacion` |
| G | Menor | `fix_sylow.py` | `git rm --cached` |
| H | Menor (nuevo) | 11 ficheros | Mover copyright antes del `import` (§23) |
| I | Menor | `NEXT-STEPS.md` | Refrescar tabla de warnings |
